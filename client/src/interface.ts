export interface PuppyInfo {
    _id: string;
    name: string,
    breed: string,
    birthDate: string
}

export interface createPuppyInfo {
    name: string,
    breed: string,
    birthDate: string
}